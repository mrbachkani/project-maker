# General Rules

These rules apply to all projects generated by the Project Maker.

- Always prefer existing components from the `components-library/` before creating new ones.
- All configuration and secrets must come from environment variables.  Do not hardcode credentials.
- Use the provided scripts and workflows for common tasks: binding backends, testing, linting, and deployment.
- Follow the directory structure conventions described in `docs/architecture-guide.md`.